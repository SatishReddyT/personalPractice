CREATE TABLE UNIVERSITY
(
U_ID INT PRIMARY KEY ,
NAME VARCHAR(255)
);




CREATE TABLE student
(
S_ID INT PRIMARY KEY,
S_NAME VARCHAR(255),
U_ID INT FOREIGN KEY REFERENCES UNIVERSITY(U_ID)
);